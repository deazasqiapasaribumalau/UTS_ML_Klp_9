services:
  - type: web
    name: uts-ml-backend
    runtime: python
    plan: free
    buildCommand: pip install -r application/backend/requirements.txt
    startCommand: cd application/backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
